# auto-generated by grc.converter

id: psk31_modulator_bc
label: PSK31 Modulator
category: '[Radioteletype]'

parameters:
-   id: samp_per_sym
    label: Samples per Symbol
    dtype: raw
    default: samp_per_sym

inputs:
-   domain: stream
    dtype: byte
    vlen: 1

outputs:
-   domain: stream
    dtype: complex
    vlen: 1

templates:
    imports: from radioteletype.modulators import psk31_modulator_bc
    make: |-
        psk31_modulator_bc(
            samp_per_sym=${samp_per_sym},
        )

file_format: 1
