# auto-generated by grc.converter

id: am_fsk_mod_bc
label: AM FSK Modulator
category: '[Radioteletype]'

parameters:
-   id: spacing
    label: Spacing
    dtype: raw
    default: '170'
-   id: samp_per_bit
    label: Samples per Bit
    dtype: raw
    default: '20'
-   id: samp_rate
    label: Sample Rate
    dtype: raw
    default: '1000'
-   id: taps
    label: Taps
    dtype: raw
    default: None

inputs:
-   domain: stream
    dtype: byte
    vlen: 1

outputs:
-   domain: stream
    dtype: complex
    vlen: 1

templates:
    imports: from radioteletype.modulators import am_fsk_mod_bc
    make: |-
        am_fsk_mod_bc(
            spacing=${spacing},
            samp_per_bit=${samp_per_bit},
            samp_rate=${samp_rate},
            taps=${taps},
        )
    callbacks:
    - set_spacing(${spacing})
    - set_samp_per_bit(${samp_per_bit})
    - set_samp_rate(${samp_rate})
    - set_taps(${taps})

file_format: 1
