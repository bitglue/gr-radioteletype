# auto-generated by grc.converter

id: radioteletype_tone_detector_cf
label: FSK Tone Detector
category: '[Radioteletype]'

parameters:
-   id: decim
    label: Decimation
    dtype: float
-   id: center_freq
    label: Center Frequency
    dtype: float
-   id: sample_rate
    label: Sample Rate
    dtype: float
-   id: baud_rate
    label: Baud Rate
    dtype: float
-   id: alpha
    label: Alpha
    dtype: float

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: float

templates:
    imports: from gnuradio.radioteletype.demodulators import tone_detector_cf \
             import gnuradio.radioteletype as radioteletype
    make: radioteletype.tone_detector_cf(${decim}, ${center_freq}, ${sample_rate},
        ${baud_rate}, ${alpha})

file_format: 1
