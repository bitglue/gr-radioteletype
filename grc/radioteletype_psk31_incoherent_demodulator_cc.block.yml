# auto-generated by grc.converter

id: psk31_incoherent_demodulator
label: PSK31 Incoherent Demodulator
category: '[Radioteletype]'

parameters:
-   id: samp_per_sym
    label: Samples per Symbol
    dtype: int
    default: samp_per_sym
-   id: sync_bandwidth
    label: Clock Sync Bandwidth
    dtype: float
    default: '0.6'
-   id: agc_time_const
    label: AGC Time Constant
    dtype: float
    default: '8.0'
-   id: sync_phases
    label: Clock Sync Phases
    dtype: int
    default: '32'

inputs:
-   domain: stream
    dtype: complex
    vlen: 1

outputs:
-   domain: stream
    dtype: complex
    vlen: 1

templates:
    imports: from radioteletype.demodulators import psk31_incoherent_demodulator_cc
    make: |-
        psk31_incoherent_demodulator_cc(
            samp_per_sym=${samp_per_sym},
            sync_bandwidth=${sync_bandwidth},
            agc_time_const=${agc_time_const},
            sync_phases=${sync_phases},
        )
    callbacks:
    - set_sync_bandwidth(${sync_bandwidth})
    - set_agc_time_constant(${agc_time_constant})

file_format: 1
