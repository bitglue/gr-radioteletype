# auto-generated by grc.converter

id: radioteletype_rtty_demod_cb
label: RTTY Demod
category: '[Radioteletype]'

parameters:
-   id: alpha
    label: Excess Bandwidth
    dtype: raw
    default: '0.35'
-   id: baud
    label: Baud Rate
    dtype: raw
    default: '45.45'
-   id: decimation
    label: Decimation
    dtype: raw
    default: '1'
-   id: samp_rate
    label: Sample Rate
    dtype: raw
    default: samp_rate
-   id: mark_freq
    label: Mark Frequency
    dtype: raw
    default: '2125'
-   id: space_freq
    label: Space Frequency
    dtype: raw
    default: '2295'

inputs:
-   domain: stream
    dtype: complex
    vlen: 1

outputs:
-   domain: stream
    dtype: byte
    vlen: 1
-   label: detector
    domain: stream
    dtype: float
    vlen: 1
-   label: mark
    domain: stream
    dtype: float
    vlen: 1
-   label: space
    domain: stream
    dtype: float
    vlen: 1

templates:
    imports: from radioteletype.demodulators import rtty_demod_cb
    make: |-
        rtty_demod_cb(
            alpha=${alpha},
            baud=${baud},
            decimation=${decimation},
            mark_freq=${mark_freq},
            samp_rate=${samp_rate},
            space_freq=${space_freq},
        )
    callbacks:
    - set_alpha(${alpha})
    - set_baud(${baud})
    - set_decimation(${decimation})
    - set_mark_freq(${mark_freq})
    - set_samp_rate(${samp_rate})
    - set_space_freq(${space_freq})

documentation: |-
    /home/indigo/.grc_gnuradio/rtty_demod.py

file_format: 1
