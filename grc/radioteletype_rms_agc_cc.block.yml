# auto-generated by grc.converter

id: rms_agc
label: RMS AGC
category: '[Radioteletype]'

parameters:
-   id: alpha
    label: Alpha
    dtype: float
    default: '0.01'

inputs:
-   domain: stream
    dtype: complex
    vlen: 1

outputs:
-   domain: stream
    dtype: complex
    vlen: 1

templates:
    imports: from radioteletype.demodulators import rms_agc_cc
    make: rms_agc_cc(alpha=${alpha})
    callbacks:
    - set_alpha(${alpha})

file_format: 1
